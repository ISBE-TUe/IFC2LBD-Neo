[package]
name = "plugin-template-export"
description = "Template: minimal export plugin for IFC2LBD-Neo (with sidecar file support)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
lbd-pipeline = { path = "../lbd-pipeline" }
# Add HTTP client or storage SDK for remote upload:
# reqwest = { version = "0.11", features = ["blocking"] }
