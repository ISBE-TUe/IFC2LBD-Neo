[package]
name = "fragments-schema"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
build = "build.rs"

[dependencies]
flatbuffers = { workspace = true }

[build-dependencies]
flatbuffers-build = { version = "=0.2.4+flatc-25.12.19", features = ["vendored"] }
