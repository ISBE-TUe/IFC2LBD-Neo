//! RML Mapper Library — Stub
//!
//! ╔══════════════════════════════════════════════════════════════════════════╗
//! ║                          INSERTION POINT                                  ║
//! ║                                                                          ║
//! ║  This crate is a placeholder. The real `rml_mapper` library source       ║
//! ║  (from the sibling `rust_rml_mapper` repo) should be copied here.         ║
//! ║                                                                          ║
//! ║  The library provides these modules (used by rml-mapper-producer):       ║
//! ║    - conformer::MappingConformer   — old RML → W3C RML conformance      ║
//! ║    - executor::Executor             — executes mapping, writes to store  ║
//! ║    - mapping::{MappingFactory, StrictMode} — parses mapping document    ║
//! ║    - store::{InMemoryQuadStore, QuadStore, RdfFormat} — RDF store       ║
//! ║                                                                          ║
//! ║  See: worker-rml-rust/src/main.rs (function execute_rml_sync)            ║
//! ║  See: plan §3.1 in docs/plan-structured-data-and-ontology-mapping.md    ║
//! ║                                                                          ║
//! ║  To install:                                                             ║
//! ║    1. Copy the rust_rml_mapper source into this directory                ║
//! ║    2. Add its dependencies to Cargo.toml                                ║
//! ║    3. Remove this stub file (or replace it with the real lib.rs)          ║
//! ╚══════════════════════════════════════════════════════════════════════════╝

/// Library version (placeholder).
pub const VERSION: &str = "0.0.0-stub";
