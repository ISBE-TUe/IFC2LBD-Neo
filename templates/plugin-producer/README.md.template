# {{display_name}}

Generated producer plugin crate template.

## Plugin Id

`{{plugin_id}}`

## What You Implement

- Add plugin-specific execution code (for example topology relation derivation).
- Keep the manifest in `src/lib.rs` aligned with inputs/outputs and failure policy.
- Register and activate via:
  - `crates/ifc2lbd-cli/src/pipeline_plugins.rs`
  - CLI flags: `--enable-plugin {{plugin_id}}`

## Validation

```bash
cargo check -p {{crate_name}}
cargo check -p ifc2lbd-cli
```
